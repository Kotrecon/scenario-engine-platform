namespace ScenarioDesigner.HealthChecks;

// ============================================================================
// АБСТРАКЦИЯ ПРОВЕРКИ БД
// Вынесена для тестирования: в тестах — мок, в проде — реальная реализация.
// ============================================================================
public interface IDatabaseHealthChecker
{
    Task CheckAsync(CancellationToken ct);
}
